collections = {"abc", "xyz", 5, (1,2,3)}

print(collections.pop())  # removes and returns an arbitrary (Random) element from the set
print("After popping an element:", collections)


# Not every time same pooping will happen, it may vary each time you run the code